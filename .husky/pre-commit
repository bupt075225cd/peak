# Pre-commit hook：提交前本地跑静态检查 + 测试 + 覆盖率门禁（与远端 CI 一致）
# 由 husky 管理，npm install 后自动启用（prepare 脚本）
# 若失败会阻断提交；可用 git commit --no-verify 跳过（不推荐，仅紧急情况）
#
# 说明：make precommit = check（Go vet + 前端类型检查）+ test（前后端 + 覆盖率）
#                       + coverage-gate（Go 70% + 逐包阈值、前端 80%）
# 覆盖率门禁依赖测试产物，因此与 push 前的 make ci 相比仅省略 build 步骤。

echo "==> pre-commit: 运行提交前检查 (make precommit)"
make precommit
